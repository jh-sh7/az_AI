# Render.com 백엔드 배포 설정 (선택)
# 대시보드에서 수동으로 Web Service 생성해도 됩니다.
services:
  - type: web
    name: az-ai-backend
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
